# MISMATCH-LOG — аудит проекта на соответствие MCP `oneentry`

Реестр несоответствий кода правилам (`get-rule`) и рецептам (`get-skill`) OneEntry SDK.
Сформирован автоматизированным аудитом: по одному агенту-аудитору на каждое правило и скилл, с адверсариальной верификацией находок вторым агентом-скептиком.

- **Дата:** 2026-07-14 (аудит) → 2026-07-15 (адверсариальная верификация завершена)
- **Область:** `app/`, `components/`, корневые конфиги. Исключены: `static-html/`, `.claude/`, `node_modules/`, `.next/`, `*.md`.
- **Измерений пройдено:** 22 правил + 20 скиллов = 42.
- **Находок:** 246 за аудит. **Осталось требующих работы: 15** в секции 3 + непроверенные пункты секции 2. Остальное закрыто и удалено из файла.
- **🔄 Сверка с текущим деревом (2026-07-18):** пройдено по всем пунктам секций 2–3. Исправлено с момента аудита — **13** пунктов (2.2, 2.12, 2.20, 2.23, 2.24, 2.25, 2.31, 2.32, 2.33, 2.37, 2.45, 2.47, 2.52 + `rule:forms`/useBookingSubmit в секции 3); частично — 2.35 и `create-checkout`/DateTimeStep. Подробности и метки «✅ ИСПРАВЛЕНО (2026-07-18)» — в шапке секции 2 и в заголовках пунктов.
- **Секция 3 = только actionable.** Разрешённые пункты из неё удалены (2026-07-17): исправленные, ⛔ опровергнутые и решённые (🔧 осознанные / 🟨 отклонённые по замеру). **Знание не потеряно** — все ⛔/🔧/🟨 перечислены в **секции 4** компактно, вместе с замерами (SDK 121 КБ gzip, картинки мастеров 52 КБ) и разбором, почему рекомендация неверна. Перед удалением машинно сверено, что секция 4 покрывает каждый пункт. Полностью закрытые измерения (15 шт.) убраны из заголовков.
- **Счётчики `осталось: N`** в заголовках измерений синхронизированы с фактом скриптом, не вручную.
- **Удалён мёртвый код (2026-07-17, по решению пользователя): 48 файлов**, недостижимых от дерева роутов — сняло 5 пунктов лога.
  - *Как найдено:* обход графа импортов от роутов (`app/**/{page,layout,loading,route,…}`) и тестов — `.claude/temp/find-dead-components.mjs`. **Грепом нельзя:** мёртвые файлы импортируют друг друга (`gallery-grid/index` → `GalleryGrid` → `GalleryCard`), поэтому «на файл есть ссылки» ничего не доказывает; нужна именно достижимость.
  - *Удалены целые ветки:* `gallery-grid/*`, `offers-table/*`, `products-table/*`, `service-hero/*`, часть `master-single/components/*`, `portfolio-grid/{PortfolioGrid,PortfolioCard,PortfolioCardImage}`, `header/search/{SearchBar,SearchResults,CloseSearch}`, `forms/index.tsx` (баррель), `useCartItem`, `getCachedData`, `OrderStatus`, `OrderStateSelect`, `OfferCircle`, `OfferInfo`, 4 иконки.
  - *⚠️ Ложные срабатывания обходчика:* `app/types/env.d.ts` и `app/types/global.d.ts` — резолвер искал `global.ts`, а не `global.d.ts`. Они ЖИВЫЕ (8 ссылок на `FormProps` из форм), исключены вручную. Урок: для `.d.ts` и ambient-типов обход по импортам не работает.
  - *Проверено:* `tsc` 0, `eslint` 0, jest 60/60, **`npm run build` успешен** — важно отдельно, потому что `dynamic()`-импорты `tsc` не ловит, — e2e 16/16. Комментарии, ссылавшиеся на удалённое (`useCartItem`, `gallery-grid`), поправлены.
- **Удалён мёртвый дубль (2026-07-17, по решению пользователя):** `components/forms/ContactUsForm.tsx` + `components/forms/inputs/FormCaptcha.tsx` — сняло **9 пунктов** разом. Компонент был недостижим (`setComponent('ContactUsForm')` не вызывался нигде), дублировал живую `ContactFormCard` (`/contacts`, та же CMS-форма `contact_us`, причём с проверкой `isError` — чего у дубля не было) и был сломан: сабмит блокировался навсегда guard'ом на токен капчи, которого заглушка `FormCaptcha` никогда не выдавала. `FormCaptcha` удалён как осиротевший — его единственным потребителем был этот дубль. Ссылки убраны из `components/forms/index.tsx` и `formsMap` модалки. Проверено: висячих ссылок нет, `tsc`/`eslint`/jest/e2e зелёные, `npm run build` успешен, форма на `/contacts` жива (4 поля + Send Message).
- **Чистка (2026-07-16, первая):** каждый неотмеченный пункт был перепроверён чтением текущего кода (16 батчей агентов + сводный spot-check). Вердикты: 64 закрыто, 36 живо, 14 наполовину, 14 неверных/про мёртвый код. Удалялись только доказанно закрытые; `🟨 ОСОЗНАННОЕ ОТКЛОНЕНИЕ` сохранено вместе с замером.
- **Сверка (2026-07-16, вторая):** при заходе на остаток обнаружилось, что **файл отстал от дерева**: 8 пунктов, помеченных «не исправлено», уже были закрыты коммитами 0.1.26–0.1.39 (включая самый опасный — пустой пароль в `authData` у `UserForm`). Все сверены чтением кода и живыми запросами к CMS, отмечены и удалены. Мораль: перед работой по пункту **сверять с текущим деревом**, а не доверять пометке в файле.
- **Частично исправленные пункты (`🟡 ЧАСТИЧНО ИСПРАВЛЕНО`) НЕ удаляются** — у них остаётся хвост работы (описан внутри пункта).

### Легенда

| Метка | Значение |
|---|---|
| ✅ CONFIRMED | Агент-скептик подтвердил нарушение (проверил и правило, и код). |
| 🟡 PARTIAL | Суть верна, severity/строка/детали уточнены при проверке. |
| ⚠️ pending | Находка аудитора без завершённой адверсариальной верификации (обрыв по лимиту). Требует ручной проверки. |
| ⛔ REFUTED | Проверкой опровергнута — к исправлению не предлагается. |
| 🔧 deliberate | Осознанное отклонение проекта (задокументировано в CLAUDE.md/коде). |

## 1. Сводка по измерениям

Осталось после чистки: закрытые находки удалены из файла (см. заголовок), в таблице — только живое.

| Измерение | Осталось открытых |
|---|:--:|
| `rule:forms` | 8 |
| `rule:jsdoc` | 9 |
| `rule:localization` | 4 |
| `rule:mismatch-log` | 1 |
| `rule:orders` | 5 |
| `rule:performance` | 4 |
| `rule:performance-bundle` | 4 |
| `rule:performance-images` | 4 |
| `rule:performance-popups` | 1 |
| `rule:performance-rtk` | 5 |
| `rule:performance-streaming` | 3 |
| `rule:playwright-e2e` | 4 | 4 | 0 | 0 | 0 |
| `rule:product-statuses` | 3 |
| `rule:server-actions` | 3 |
| `skill:create-auth` | 3 |
| `skill:create-cart-manager` | 2 |
| `skill:create-checkout` | 4 |
| `skill:create-form` | 4 |
| `skill:create-menu` | 4 |
| `skill:create-orders-list` | 2 |
| `skill:create-page` | 1 |
| `skill:create-product-card` | 3 |
| `skill:create-product-list` | 3 |
| `skill:create-profile` | 5 |
| `skill:create-search` | 2 |
| `skill:create-server-action` | 3 |
| `skill:setup-oneentry` | 1 |

## 2. Подтверждённые нарушения (верифицированы)

Отсортировано по критичности. Каждый пункт прошёл независимую адверсариальную проверку.

**🔄 Сверка с кодом (2026-07-18).** Все пункты секций 2–3 пройдены против текущего дерева (чтением кода + агенты-верификаторы). **Исправлено с момента аудита:** 2.2 (галерея из CMS), 2.12 (UTC-интервал), 2.20 (window-guard в saveFunction), 2.23 (fileUrl → общий `fileDisplayUrl` с guard), 2.24 (home: `notFound()` вместо строки «isError»), 2.25 (profile: `Promise.all`), 2.31 (isSignUpRequired в видимости полей), 2.32 (`FormField = IFormAttribute`), 2.33 (JSDoc `isConfirmPasswordField`), 2.37 (logout: маркер из localStorage), 2.45 (`getFormAttributes` дефолт `IFormAttribute`), 2.47 (layout: `Promise.all`), 2.52 (файл `getImageSize.ts` удалён при чистке мёртвого кода); в секции 3 — `rule:forms`/useBookingSubmit (salon = числовой id, interval = ISO-строки). **Частично исправлено:** 2.35 (пароль убран из formData, но пустые значения `|| ''` и роутинг по имени маркера остались); `skill:create-checkout`/DateTimeStep (слоты теперь читаются из `master_schedule`/`salon_time` через `daySlots`, статический `TIMES` — только фолбэк); `skill:create-checkout` маркеры вынесены в `orderMarkers.ts` (динамический путь всё ещё нежизнеспособен). **Остальные подтверждены как актуальные** — многие помечены 🔧 (осознанные) или ИНФО «оставить как есть». Заголовки исправленных ниже несут метку «✅ ИСПРАВЛЕНО (2026-07-18)».

### 2.22. [НЕЗНАЧИТЕЛЬНО] ✅ 🔧 `app/contacts/page.tsx:119`

*Измерение: `rule:nextjs-pages`*

**Суть:** Ряд UI-текстов и данных секций захардкожен в page-файлах без чтения из CMS/словаря: «Get in Touch», подзаголовок героя, статистика «Daily 10:00–22:00» и др.

**Детали:** Примеры в файлах правила (app/**/page.tsx): contacts:119 — заголовок секции «Get in Touch» (SectionHeading), contacts:98 — подзаголовок «… · Always happy to see you», contacts:104–108 — статистика ['Daily','10:00–22:00'], ['Dubai','UAE']; offers:70 — «Back to Home», offers:100–101 — «Good to know» и offerTermsData из components/data; booking:41 — «Premium beauty experience»; services:46 — «locations across Dubai»; salons/[handle]:62 — about/highlights из локального salonContent.ts. Заголовки h1 при этом везде берутся из CMS (page.localizeInfos.title) — нарушение касается вторичных текстов. Это соответствует задокументированному переходному состоянию портирования: components/data.js описан в CLAUDE.md как «временные захардкоженные данные до переноса в CMS», атрибут salon_time заведён в CMS, но кодом пока не читается, а у словарных UI-текстов проект требует лишь английские фолбэки.

**Рекомендация:** По мере наполнения CMS переносить эти тексты в system_content (getDictionary с английскими фолбэками) и атрибуты страниц (например, часы работы — из salon_time), начиная с повторяющихся текстов (часы «10:00–22:00» встречаются и в contacts, и в OpeningHours).

> Проверка: Правило nextjs-pages действительно содержит раздел «DO NOT hardcode page content» (цитата в находке точная), и его paths покрывают app/**/page.tsx. Все file:line подтверждены дословно: contacts:119 «Get in Touch», contacts:98 «Always happy to see you», contacts:105–107 stats ['Daily','10:00–22:00']/['Dubai','UAE']; offers:70 «Back to Home», offers:101 «Good to know» + offerTermsData из components/data.js; booking:41 «Premium beauty experience»; services:46 «locations across Dubai»; salons/[handle]:62 SALON_CONTENT из локального salonContent.ts. Обработки в другом месте нет: getDictionary() вызывается, но ни одна из этих строк из словаря не читается — «Get in Touch» существует только как JSX-литерал, паттерн dict?.x?.value || '…' для них отсутствует. Утверждение находки, что h1 везде из CMS и нарушение касается только вторичных текстов, тоже подтверждено. Severity minor корректна: нарушение буквы правила реально, но это задокументированное переходное состояние проекта (components/data.js — «временные данные до переноса в CMS», salon_time заведён, но не читается), что находка честно отражает (deliberate: true).

### 2.26. [НЕЗНАЧИТЕЛЬНО] ✅ 🔧 `app/reviews/page.tsx:32`

*Измерение: `rule:nextjs-pages`*

**Суть:** Тело страницы /reviews целиком рендерится из локального мока (components/layout/reviews-page/data.ts), контент CMS-страницы 'reviews' в теле не используется (только в generateMetadata).

**Детали:** ReviewsPageLayout не вызывает getPageByUrl для тела страницы: заголовок «Reviews» захардкожен в components/layout/reviews-page/index.tsx:135, отзывы — из локального массива REVIEWS (data.ts). Это нарушает «DO NOT hardcode page content», но соответствует задокументированному состоянию проекта: блок reviews_carousel в CMS создан, но пуст (без атрибутов/слайдов), и отзывы фронта осознанно держатся на моке до наполнения CMS.

**Рекомендация:** После наполнения reviews_carousel/страницы reviews в CMS перевести заголовок на page.localizeInfos?.title и отзывы на данные CMS; до тех пор — оставить как задокументированное переходное состояние.

> Проверка: Правило nextjs-pages действительно содержит секцию «DO NOT hardcode page content» (пример: хардкод <h1> запрещён, нужен page.localizeInfos?.title) — цитата точна. Код подтверждается полностью: app/reviews/page.tsx:32 рендерит тело только из <ReviewsPageContent/>, getPageByUrl('reviews') используется лишь в generateMetadata (строка 47); заголовок «Reviews» захардкожен в components/layout/reviews-page/index.tsx:135; отзывы берутся из локального мока components/layout/reviews-page/data.ts (REVIEWS/REVIEW_SALONS/MASTER_SALON/MASTER_CAT). Никакой обработки/фолбэка на CMS в другом месте нет (grep по app/ — ноль CMS-источников для тела /reviews). Состояние осознанное и задокументированное (JSDoc страницы, шапка data.ts, CLAUDE.md: reviews_carousel пуст), поэтому deliberate:true и severity minor корректны. Формальное нарушение правила реально — CONFIRMED.

### 2.46. [ИНФО] ✅ `app/api/api/api.ts:58`

*Измерение: `rule:typescript`*

**Суть:** hasActiveSession читает внутреннее поле AuthProvider.state через 'as unknown as { state?: { accessToken?: string } }' — публичный тип SDK его не описывает.

**Детали:** Каст задокументирован JSDoc-комментарием (SDK хранит auth-состояние внутри AuthProvider, публичного геттера нет), что укладывается в исключение правила о конфликте типа SDK и фактического поведения. Риск: поле state — недокументированный интерьер SDK и может исчезнуть/переименоваться при обновлении пакета, а двойной каст скроет поломку на уровне типов (вернётся просто false).

**Рекомендация:** Оставить с текущей документацией; при обновлениях SDK проверять, не появился ли публичный метод проверки сессии (например, isAuth/getAccessToken), и мигрировать на него.

> Проверка: Факты подтверждены полностью. (1) file:line точны: каст `as unknown as { state?: { accessToken?: string } }` — app/api/api/api.ts:58, JSDoc-обоснование на строках 50-56. (2) Публичный тип SDK действительно не даёт доступа к полю: в authProviderApi.d.ts/asyncModules.d.ts оно объявлено как `protected state: StateModule` — извне без каста недоступно; при этом форма каста совпадает с реальным типом (StateModule.accessToken: string | undefined). (3) Публичной альтернативы нет: grep по всем .d.ts SDK не находит getAccessToken/isAuth/hasSession (getActiveSessionsByMarker — сетевой вызов, не локальная проверка), так что рекомендация «оставить и следить за обновлениями SDK» корректна. (4) Риск описан верно: двойной каст скроет переименование `state` на уровне типов, функция молча вернёт false (вызывающий AuthContext.onInit:144 просто сделает лишний reDefine — мягкая деградация, что подтверждает severity info). (5) Правило typescript запрещает `any` и DTO-дубли — здесь ни того, ни другого; цитируемое исключение («document this explicitly») в правиле буквально описывает обратный случай (тип требует поле, API его отвергает), так что привязка — интерпретация принципа «задокументированного отклонения», но находка и не заявляет нарушения (severity info, deliberate false), и её суть, строка и severity точны. Обработки/нормализации в другом месте нет — находка не устарела.

### 2.48. [ИНФО] ✅ `app/store/providers/AuthContext.tsx:171`

*Измерение: `rule:tokens`*

**Суть:** Ручные localStorage.setItem('refresh-token') и syncTokens после auth() избыточны по правилу

**Детали:** login() (строки 162–176) вызывается формами после успешного `AuthProvider.auth()` (SignInForm.tsx:113–124, SignUpForm.tsx:211–228, VerificationForm.tsx:102–115). По правилу auth() сам кладёт оба токена в state текущего инстанса и вызывает saveFunction — ручной `localStorage.setItem('refresh-token', …)` (строка 171) и `syncTokens(…)` (строка 173) после него избыточны (записывают те же значения). Вреда нет — операции идемпотентны; сохранение authProviderMarker (строка 172) при этом обязательно и корректно.

**Рекомендация:** Можно упростить login() до сохранения authProviderMarker + setIsAuth + checkToken, как в эталоне правила. Если syncTokens оставлен намеренно как страховка единого пути для auth/signUp — зафиксировать это в JSDoc со ссылкой на правило.

> Проверка: Правило tokens дословно содержит заявленное: после auth() «syncTokens and manual localStorage.setItem('refresh-token', ...) after it are redundant». file:line точны: AuthContext.tsx:171 — setItem('refresh-token'), :173 — syncTokens внутри login() (162–176). Адверсариальные проверки не опровергли: (1) saveFunction сконфигурирован в SDK-инстансе проекта (app/api/api/api.ts:22–38) и пишет refresh-token в localStorage — auth() сам персистит токен; (2) SDK oneentry@1.0.155 ≥ 1.0.152 — поведение правила применимо; (3) все три вызова login() (SignInForm.tsx:113→120, SignUpForm.tsx:211→224, VerificationForm.tsx:102→111) идут только после AuthProvider.auth(); oauth() в проекте нет, исключение правила не применяется. Находка честно отмечает идемпотентность (вреда нет) и обязательность сохранения authProviderMarker. Severity info адекватна — это избыточность, не баг.

### 2.49. [ИНФО] ✅ `app/store/providers/AuthContext.tsx:171`

*Измерение: `rule:auth-provider`*

**Суть:** login() вручную дублирует сохранение refresh-токена в localStorage и запись токенов в SDK, хотя auth() уже делает это сам через saveFunction

**Детали:** localStorage.setItem('refresh-token', refreshToken) (строка 171) и syncTokens(accessToken, refreshToken) (строка 173, app/api/api/api.ts:73–76) дублируют то, что auth()/signUp() уже сделали: SDK кладёт оба токена в state того же инстанса и вызывает saveFunction, пишущую refresh-токен под тем же ключом 'refresh-token' (app/api/api/api.ts:22–27). Вреда нет (ключ совпадает, accessToken в localStorage не пишется, authProviderMarker сохраняется корректно), но по правилу ручное сохранение не нужно — достаточно сохранить маркер провайдера.

**Рекомендация:** Оставить в login() только сохранение authProviderMarker и обновление React-состояния; ручную запись 'refresh-token' и syncTokens можно убрать (предварительно проверив, что нигде не полагаются на синхронность записи).

> Проверка: Правило auth-provider дословно содержит процитированное требование («tokens do not need to be saved manually … Only save the provider marker»). SDK подтверждает (node_modules/oneentry/dist/auth-provider/authProviderApi.js:191–203): auth() сам кладёт оба токена в state инстанса и вызывает saveFunction, а saveFunction проекта (app/api/api/api.ts:22–27) пишет refresh-токен под тем же ключом 'refresh-token' и сконфигурирована во всех defineOneEntry. Все три вызова login() (SignInForm:113→120, VerificationForm:102→111, SignUpForm:211→224) идут сразу после getApi().AuthProvider.auth() на том же инстансе — значит localStorage.setItem('refresh-token', …) (AuthContext.tsx:171) и syncTokens (строка 173) действительно дублируют уже сделанное SDK. file:line точны, severity info корректна (вреда нет: ключ совпадает, accessToken в localStorage не пишется, маркер сохраняется). Единственная микронеточность в details: сам signUp() saveFunction не вызывает, но в проекте после signUp всегда вызывается auth() перед login(), так что суть находки не затронута.

### 2.50. [ИНФО] ✅ 🔧 `app/store/providers/AuthContext.tsx:233`

*Измерение: `rule:linting`*

**Суть:** Осознанное подавление exhaustive-deps с задокументированным обоснованием (isAuth намеренно исключён из deps)

**Детали:** useEffect «Check token on refetch» (строки 227–234) читает isAuth, но deps — [refetch, refetchUser, checkToken]; подавление снабжено комментарием (строки 231–232): перепроверка токена нужна только при явном переключении refetch/refetchUser, а не при каждой смене auth-состояния. Формально нарушает требование правила о полном списке зависимостей, но решение зафиксировано в коде и функционально обосновано (иначе — лишние перепроверки токена при каждом логине/логауте).

**Рекомендация:** Оставить как есть (обоснование в коде присутствует). Альтернатива без подавления — хранить isAuth в ref и читать его внутри эффекта.

> Проверка: Правило "linting" дословно требует: «react-hooks/exhaustive-deps — all dependencies of useEffect must be in the deps array» — цитата в находке точна. В AuthContext.tsx эффект (строки 227–234) читает isAuth, но deps = [refetch, refetchUser, checkToken]; подавление eslint-disable-next-line стоит ровно на строке 233, поясняющий комментарий — строки 231–232. Обоснование комментария соответствует коду: refetch/refetchUser переключаются только в authenticate()/refreshUser(), а добавление isAuth в deps дало бы лишние checkToken() при каждом логине/логауте (login() уже вызывает checkToken сам). Обработки «в другом месте» для линт-подавления не существует по определению. Severity info и deliberate=true адекватны: нарушение формальное, осознанное, задокументированное, ESLint с директивой проходит. Все детали находки подтверждены.

### 2.51. [ИНФО] ✅ `app/types/global.d.ts:4`

*Измерение: `rule:typescript`*

**Суть:** Объявлен неиспользуемый тип LocalizeInfo — нигде в app/ и components/ не употребляется и по смыслу дублирует SDK-тип ILocalizeInfo.

**Детали:** declare type LocalizeInfo = { content; menuTitle; title } не имеет ни одного использования (grep по всей области аудита — только само объявление) и представляет устаревший ручной слепок SDK-типа ILocalizeInfo ('oneentry/dist/base/utils'), причём с расходящейся формой (content вместо plainValue/htmlValue/htmlContent). ESLint не флагует ambient-объявления в .d.ts, поэтому no-unused-vars это не ловит.

**Рекомендация:** По духу правила тип подлежит удалению, но по конвенции проекта («ничего не удалять без явной просьбы») — согласовать удаление с пользователем; при будущем использовании локализаций импортировать ILocalizeInfo из SDK.

> Проверка: Опровергнуть не удалось, все утверждения находки подтверждены первоисточниками. (1) Правило "typescript" содержит точную цитату "Do not declare unused variables and imports" и дополнительно раздел о запрете дублирования SDK-типов, где ILocalizeInfo прямо указан в таблице импортов из 'oneentry/dist/base/utils'. (2) file:line верны: declare type LocalizeInfo = { content; menuTitle; title } в app/types/global.d.ts:4-8. (3) Grep по всему проекту (вне node_modules/.next/static-html): единственное вхождение голого LocalizeInfo — само объявление; gallery-feed использует SDK-тип ILocalizeInfo напрямую, минуя ручной. (4) Форма действительно расходится: SDK ILocalizeInfo = { title, plainValue?, htmlValue?, htmlContent?, menuTitle? }, поля content нет. (5) Severity info адекватен (нет рантайм-эффекта), рекомендация корректно учитывает конвенцию проекта «ничего не удалять без явной просьбы».

### 2.53. [ИНФО] ✅ `components/layout/home/home-hero/index.tsx:41`

*Измерение: `rule:typescript`*

**Суть:** slide.attributeValues кастится через 'as unknown as Record<string, unknown>', переопределяя SDK-тип IAttributeValues (у слайдов API реально отдаёт сырые значения без обёртки {value}).

**Детали:** SDK типизирует IBlockSlideItem.attributeValues как IAttributeValues (Record<string, { type, value, … }>), но фактический ответ эндпоинта слайдов содержит сырые значения (массив файлов / строку) — это расхождение SDK-типа и поведения API. Код документирует его развёрнутым комментарием (строки 33–40), что соответствует исключению правила («document this explicitly»), поэтому это не нарушение, а зона риска: при исправлении типа в будущих версиях SDK двойной каст скроет изменение.

**Рекомендация:** Оставить как есть (исключение задокументировано), но зафиксировать расхождение в mismatch-log (MCP-правило mismatch-log) и при обновлении пакета oneentry перепроверять фактическую форму ответа getSlides.

> Проверка: Все утверждения находки подтверждены первоисточниками. (1) Правило typescript действительно содержит и анти-паттерн «Retyping attributeValues — duplicates IAttributeValues», и исключение «Exception — SDK type conflict and API behavior … document this explicitly»; ruleQuote — реальный текст правила. (2) file:line точны: components/layout/home/home-hero/index.tsx:41-42 — именно двойной каст `as unknown as Record<string, unknown>`. (3) SDK-тип подтверждён: IBlockSlideItem.attributeValues: IAttributeValues (blocksInterfaces.d.ts:344), где IAttributeValue = {type, value, …}. (4) Живой вызов Blocks.getSlides('home_hero') подтвердил: API отдаёт сырые значения (image_id1 — массив файлов с downloadLink, string_id3 — строка) без обёртки {value} — расхождение SDK-типа и API реально. (5) Расхождение задокументировано комментарием (строки 33-40) и JSDoc обёртки getBlockSlides.ts — код соответствует исключению правила, что находка и констатирует (severity info, «не нарушение, а зона риска»). (6) Нормализации в другом месте нет: getBlockSlides — pass-through, единственный потребитель — этот файл; MISMATCH-LOG.md в репо отсутствует, т.е. рекомендация зафиксировать расхождение актуальна. Единственная микро-неточность — цитируемое исключение буквально про request-side (400 при передаче поля), а здесь response-shape, но заголовок исключения («SDK type conflict and API behavior») случай покрывает; на суть и severity не влияет. Опровергнуть находку не удалось.

### 2.54. [ИНФО] 🟡 `components/layout/service-hero/components/ServiceDescription.tsx:13`

*Измерение: `rule:attribute-values`*

**Суть:** Доступ к entity.attributeValues без предохранителя || {} в нескольких компонентах (деструктуризация/прямое обращение)

**Детали:** Правило предписывает паттерн const attrs = entity.attributeValues || {}. Без него: ServiceDescription.tsx:13, ServiceTitle.tsx:13, ServiceImage.tsx:13 (деструктуризация page.attributeValues), MasterInfo.tsx:31, MasterReviews.tsx:23 (master.attributeValues), portfolio-grid/index.tsx:48 (master.attributeValues.master_portfolio при том, что стр. 92 того же файла использует ?? {}). Сейчас API стабильно возвращает attributeValues (у страниц — пустой объект), поэтому эффекта нет; при недоступности поля был бы TypeError вместо предписанной тихой деградации.

**Рекомендация:** Привести к паттерну правила: const attrs = entity.attributeValues || {} (или ?.) во всех перечисленных местах — как уже сделано в catalog-data.ts, booking-data.ts, contacts/page.tsx.

> Проверка: Правило attribute-values действительно предписывает паттерн `const attrs = entity.attributeValues || {};` — цитата точна. Все 6 указанных file:line подтверждены дословно, нормализации выше по стеку нет (обёртки app/api/server attributeValues не трогают). НО: 5 из 6 мест — мёртвый код. ServiceHero (service-hero/index.tsx) не импортируется нигде, значит ServiceDescription/ServiceTitle/ServiceImage не рендерятся; MasterInfo.tsx и MasterReviews.tsx импортируются только из Master.tsx, который сам нигде не используется (живой master-single/index.tsx уже применяет `?? {}` на стр. 57). Единственное живое место из списка — portfolio-grid/index.tsx:48 (используется в app/masters/[handle]/page.tsx), причём там есть и второй незащищённый доступ на стр. 66 (page.attributeValues.gallery_photos), не упомянутый в находке. Также пропущено живое нарушение: master-single/components/MasterDescription.tsx:20 (рендерится из живого index.tsx:167). Суть находки верна (отступление от паттерна правила, эффекта сейчас нет, severity info корректна), но детализация существенно неточна: большинство перечисленных мест никогда не исполняется, а часть живых нарушений не перечислена.

## 3. Находки второй волны — адверсариальная верификация (прогон 2026-07-15)

Верификация завершена: **191/191** проверено адверсариально (✅ 129 подтверждено, 🟡 28 уточнено, ⛔ 8 опровергнуто, ✅ ✔️ 26 уже исправлено в текущем дереве).
Метки: ✅ подтверждено · 🟡 суть верна, детали/severity уточнены · ⛔ опровергнуто · ✅ ✔️ИСПРАВЛЕНО — было верно на момент аудита, в текущем дереве уже устранено · 🔧 осознанное отклонение (disposition «оставить как есть» не меняется).

> **🔄 Обработано в сессии 2026-07-18 («делай это» по всему разделу 3):**
>
> - **`rule:forms` (1/1)** — ✔️ `ForgotPasswordForm` сортирует поля `sortArrayByPosition(getFormAttributes(...))` (эффект нулевой — одно поле, но правило закрыто).
> - **`skill:create-auth` (1/1)** — ✔️ `SignUpForm` берёт `providerMarker`/`formIdentifier` из `useGetAuthProvidersQuery` (credential-провайдер; фолбэк `email`/`reg` = текущие CMS-значения); хардкод убран, поведение сохранено. Смоук: форма рендерится.
> - **`skill:create-cart-manager` (1/1)** — ✔️ вводящий в заблуждение комментарий «Load cart from user state» в `AuthContext` переписан (это флаг-заглушка, не загрузчик); `updateUserState`/`clearUserState` помечены незадействованным legacy. Мёртвый код НЕ удалён (конвенция).
> - **`skill:create-checkout` (2/2)** — ✔️ маркеры полей заказа (`master`/`salon`/`interval`) вынесены в `orderMarkers.ts` (`ORDER_FIELD_*`). Динамический путь (formIdentifier из storage / поля из формы) НЕ применён — по живой проверке CMS он сломал бы заказ (`storage.formIdentifier=null`, форма `order` без полей). DateTimeStep уже schedule-driven — без изменений.
> - **`skill:create-product-list` (1/1)** — ✔️ RTK `getProducts`/`getProductsByPageUrl` получают `offset`/`limit` через `userQuery` (по образцу серверных обёрток); `{ error: null }` при пустом url → пустой список. Хуки не потребляются (мёртвый код), но латентный дефект устранён.
> - **`rule:performance-rtk` (1/1)** — 🔧 задокументировано: локальный `user`-state в `AuthContext` — осознанный decoupling от 60с-поллинга getMe (буквальный фикс «читать data из хука» дал бы ре-рендер всех потребителей каждые 60с).
> - **`rule:performance-streaming` (1/1)** — 🔧 задокументировано: `booking/page` — `force-static` (пререндер); тяжёлый `getBookingData()` выполняется при прегенерации, не на запрос; локальный `<Suspense>` на статической странице пользователю никогда не покажется → рефактор бессмыслен (в отличие от `masters/[handle]`, который `revalidate`-only).
> - **`rule:jsdoc` (7/7)** — ✔️ JSDoc добавлен именованным функциям: 14 хендлеров `useBookingWizard`, `home-hero` `fileLink`/`str`, `findValue`, `applyPatch`, `worker`, `percentile`, расширен `SignOutButton.handleLogout`. Примеры `fetchOrders`/`fetchPages`×2 устарели (функции отрефакторены, в дереве отсутствуют). Масс-стилевые нюансы (тип в `@returns`/`@param`, первая строка `Name —`, пустая строка перед `@param`) — 🔧 зафиксированы как осознанный стиль проекта в `CLAUDE.md` (тип в фигурных скобках у обоих тегов — канон; без масс-кодмода).
>
> Проверка сессии: `npm run typecheck` — 0 · `eslint` (14 файлов) — 0 · `jest` — 127/127 (доустановлен недостающий peer `@testing-library/dom@^10` — из-за его отсутствия ранее не грузились 4 компонентных сьюта) · Playwright: `/booking` и auth `SignIn→SignUp` рендерятся чисто.

### `rule:forms` — осталось: 0 ✅ (закрыто 2026-07-18)

- **[НЕЗНАЧИТЕЛЬНО]** ✅ 🟡 ЧАСТИЧНО ИСПРАВЛЕНО (сверено 2026-07-16) `components/forms/SignUpForm.tsx:84` — Поля формы не сортируются по position в SignUpForm, UserForm и ForgotPasswordForm.
  - *Сделано:* `SignUpForm.tsx:99` и `UserForm.tsx:56` используют `sortArrayByPosition(getFormAttributes(...))`. Остался только `ForgotPasswordForm.tsx:107` — там `filter` оставляет **единственное** поле `email_reg`, так что сортировать нечего и эффект строго нулевой; закрыть при следующей правке файла.
  - *Рек.:* Добавить .sort((a, b) => a.position - b.position) (или sortArrayByPosition из components/utils.ts) после getFormAttributes во всех трёх формах.
  > Проверка: Правило forms.md явно требует сортировку полей: «attributes: IFormAttribute[] — form fields for rendering. Sort by position». В текущем рабочем дереве SignUpForm.tsx:84 берёт getFormAttributes<FormField>(data) без .sort, дальше только filter → рендер в порядке API; UserForm.tsx:56 и :148 — то же самое; ForgotPasswordForm.tsx:92 тоже без сортировки (хотя там filter оставляет единственное поле email_reg, так что эффект нулевой). Компенсации в другом месте нет: getFormAttributes (components/utils.ts:20-25) возвращает shallow copy без сортировки (её JSDoc-пример прямо предлагает сортировать вызывающему), RTK-эндпоинт getFormByMarker (app/api/api/RTKApi.ts:269) и серверная обёртка app/api/server/forms/getFormByMarker.ts отдают ответ SDK как есть. Соседние SignInForm.tsx:80 и ContactUsForm.tsx:42 сортируют — конвенция проекта подтверждает требование. Незакоммиченные правки 14–15 июля затронули только SignInForm/VerificationForm/FormInput, три названные формы не исправлены. Severity minor адекватна: видимого эффекта сейчас нет (у формы reg в CMS attributes вообще пустой объект, а API обычно и так отдаёт поля по position), но нарушение правила реально.

### `rule:jsdoc` — осталось: 0 ✅ (2026-07-18: локальные — JSDoc добавлен; масс-стиль — 🔧 зафиксирован в CLAUDE.md)

- **[СЕРЬЁЗНО]** 🟡 `components/utils.ts:14` — Систематическое указание типа в @returns в фигурных скобках — 421 из 442 тегов @returns по всей базе (app/ и components/) нарушают требование «@returns без типа».
  - *Рек.:* Определиться на уровне проекта: либо массово убрать типы из @returns (`@returns {JSX.Element} X` → `@returns X`) кодмодом/eslint-правилом jsdoc/no-types для @returns, либо зафиксировать отклонение от правила jsdoc в документации проекта. Как минимум — не воспроизводить тип в @returns в новых файлах (SaleText.tsx уже создан с нарушением).
  > Проверка: Правило jsdoc.md действительно требует: «@returns Description. — always when there is a return value, WITHOUT type in curly braces» (пп. 6 и раздел Types). Код по file:line подтверждается: components/utils.ts:14 — `@returns {Array} Shallow copy...`; по всей базе (app/ + components/) сейчас 422 из 443 тегов @returns содержат тип в фигурных скобках (в находке 421/442 — расхождение на 1 из-за правок дерева после аудита, суть не меняет). SaleText.tsx:9 тоже содержит `@returns {JSX.Element}` — упоминание в рекомендации верно. Незакоммиченные правки форм (SignInForm/VerificationForm/FormInput) тегов @returns не касались — не ALREADY_FIXED. Обработки/нормализации «в другом месте» для стилистического правила быть не может; в списке осознанных отклонений проекта JSDoc-стиль не зафиксирован, хотя CLAUDE.md формулой «стиль существующих файлов» фактически закрепляет текущий стиль. Однако severity major завышена: по шкале major — «реальный эффект для пользователя/данных», а тип в @returns — чисто документационное отклонение без какого-либо рантайм-эффекта; это классический minor («нарушение без видимого эффекта сейчас»), к тому же само правило помечено как Optional. **Уточнение:** Систематическое указание типа в @returns в фигурных скобках — 422 из 443 тегов @returns по app/ и components/ нарушают требование правила jsdoc «@returns без типа»; отклонение единообразное и чисто документационное, без рантайм-эффекта. (severity → НЕЗНАЧИТЕЛЬНО)
- **[СЕРЬЁЗНО]** 🟡 `app/api/api/RTKApi.ts:79` — 29 тегов @param без типа в фигурных скобках: все JSDoc эндпоинтов RTKApi.ts (20 тегов), OpenDrawerContext.tsx (8) и components/utils.ts:154.
  - *Рек.:* Дописать типы в фигурных скобках всем @param в RTKApi.ts и utils.ts:154. В OpenDrawerContext.tsx заменить псевдо-@param-список на обычное описание полей контекста (JSDoc с @param — для функций, не для типов/контекстов).
  > Проверка: Источник (jsdoc.md) действительно требует заявленного: «All @param must have a type in curly braces — even if that type is already in the TypeScript signature», а для типов — «JSDoc is needed for functions, not types». Grep по текущему дереву подтверждает ровно 29 тегов @param без {Type}: 20 в app/api/api/RTKApi.ts (первый — строка 79, file:line точен), 8 в app/store/providers/OpenDrawerContext.tsx:8-15 (псевдо-@param на createContext, т.е. на типе — рекомендация верна) и 1 в components/utils.ts, но теперь на строке 188, а не 154: коммит 0.1.24 (b6f7020, 14.07.2026 20:56) добавил 34 строки выше, на момент аудита строка 154 была верна. Нормализации/обработки «в другом месте» для документационного нарушения не существует по определению; других файлов с такими нарушениями нет — инвентаризация полная. Ничего из перечисленного не исправлено. Однако severity major завышена: это чисто оформление JSDoc без какого-либо эффекта для пользователя или данных — по шкале это minor («нарушение без видимого эффекта сейчас»). **Уточнение:** 29 тегов @param без типа в фигурных скобках: все JSDoc эндпоинтов RTKApi.ts (20 тегов), OpenDrawerContext.tsx:8-15 (8, псевдо-@param на типе контекста) и components/utils.ts:188 (sortObjectFieldsByPosition; на момент аудита — строка 154, сместилась коммитом 0.1.24). (severity → НЕЗНАЧИТЕЛЬНО)
- **[НЕЗНАЧИТЕЛЬНО]** ✅ `components/layout/booking-page/useBookingWizard.ts:329` — 14 именованных хендлеров хука useBookingWizard (handleNext, handleBack, resetFlow, startFlow, selectSalon, selectService и др.) без индивидуальных JSDoc — только групповой баннер-комментарий.
  - *Рек.:* Добавить короткие JSDoc-блоки каждому хендлеру (у принимающих аргументы — с @param {string} id и т.п.).
  > Проверка: Правило jsdoc.md требует JSDoc-блок над каждой объявленной функцией, включая хендлеры («Every declared function … handler, exported or not — must be accompanied by a JSDoc block above the declaration»); исключение действует только для колбэков без отдельного именованного объявления. В components/layout/booking-page/useBookingWizard.ts после группового баннера «── Handlers (mock BookingPage handlers) ──» (строка 327) объявлены ровно 14 именованных хендлеров без индивидуальных JSDoc: handleNext (329), handleBack, resetFlow, startFlow, selectSalon, selectService, clearService, selectMaster, handleConfirm, handleCloseSuccess, goStep, onCategoryChange, onDate, onTime — счёт и строка из находки подтверждаются. Описания хендлеров присутствуют только как комментарии к полям интерфейса BookingWizardState, но правило прямо оговаривает, что JSDoc нужен функциям, а не типам, поэтому это требование не закрывает. Файл не менялся в правках 0.1.23–0.1.24 и не входит в незакоммиченные изменения рабочего дерева — нарушение актуально сейчас. Severity minor адекватна: чисто документационное нарушение без видимого эффекта для пользователя.
- **[НЕЗНАЧИТЕЛЬНО]** ✅ `components/layout/home/home-hero/index.tsx:43` — Около 20 прочих именованных внутренних функций/хендлеров без JSDoc в разных файлах (fileLink/str, fetchOrders, fetchPages, applyPatch, findValue и др.).
  - *Рек.:* Добавить короткие JSDoc минимум функциям с нетривиальной логикой (fileLink/str, fetchOrders, fetchPages, applyPatch, findValue, worker, percentile); для одно-строчных коллбэков внутри useEffect допустимо оставить как есть, зафиксировав трактовку в конвенциях проекта.
  > Проверка: Правило jsdoc.md требует JSDoc для каждой объявленной функции («Every declared function in the project — … exported or not — must be accompanied by a JSDoc block»); исключение только для коллбэков в useEffect/.map()/onClick «if they do not have a separate named declaration» — на именованные объявления оно не распространяется. Якорь точен: в components/layout/home/home-hero/index.tsx на строках 43 и 48 именованные функции fileLink и str без JSDoc (комментарий выше на строках 33–40 относится к attrs, а не к ним). Все перечисленные примеры подтверждены в текущем дереве: fetchOrders (ProfileHistory.tsx:49), fetchPages (SearchResults.tsx:50, SearchResultsList.tsx:63), applyPatch (CartSlice.ts:113), findValue (updateUserState.ts:43), worker (ApiTestClient.tsx:289), percentile (statsUtils.ts:39). Дополнительный grep находит ещё около десятка таких же случаев (getProductsByIds в RTKApi.ts:138, handleNext/handleBack/handleConfirm/handleCloseSuccess/onCategoryChange/onDate/onTime в useBookingWizard.ts:329–434, onLocScroll, onKey и др.) — оценка «около 20» реалистична. Нормализации «в другом месте» для документационного правила быть не может, а правки 0.1.23–0.1.24 и незакоммиченные изменения форм эти файлы не затрагивают. Severity minor адекватна: нарушение стиля документации без видимого эффекта для пользователя.
- **[НЕЗНАЧИТЕЛЬНО]** ✅ `app/api/api/api.ts:14` — Первая строка JSDoc почти нигде не следует формату «Name — what it does.» с именем функции и em-dash — описания начинаются с глагольной фразы без имени.
  - *Рек.:* В новых и редактируемых файлах начинать первую строку с имени функции и em-dash: «SaleText — renders sale text with % wrapped in spans.». Массовый рефакторинг существующих блоков — по решению команды.
  > Проверка: Правило jsdoc.md, раздел Block structure, п.1 требует: «First line — a short description using an em-dash: Name — what it does.». В app/api/api/api.ts:14 первая строка JSDoc — «Save refresh token to localStorage.» (глагольная фраза без имени и em-dash), и все остальные блоки файла оформлены так же. Подсчёт по проекту (app/ + components/): из 619 первых строк многострочных JSDoc формату «Name — » соответствуют лишь ~75 (~12%), в основном в недавно перевёрстанных файлах (booking, reviews, masters, gallery) — «почти нигде» подтверждается. Нормализации/обработки в другом месте здесь быть не может — это стилевое требование к самим блокам. Незакоммиченные правки рабочего дерева (SignInForm/VerificationForm/FormInput) файл api.ts не затрагивают — проблема актуальна на текущий момент. Severity minor адекватна: нарушение нормативного формата без видимого эффекта для пользователя; правило в источнике optional, но проектный CLAUDE.md требует JSDoc у каждой функции, так что правило применимо.
- **[НЕЗНАЧИТЕЛЬНО]** ✅ `components/utils.ts:13` — Систематически отсутствует пустая строка между описанием/расширенным контекстом и секцией @param.
  - *Рек.:* Добавлять пустую комментарную строку `*` перед первым @param в новых/редактируемых блоках; массовое исправление можно сделать кодмодом.
  > Проверка: Правило jsdoc.md в разделе «Block structure» прямо требует пустую строку после описания и после расширенного контекста перед @param («2. An empty line. 3. (optional) Extended context ... Then another empty line.»), что подтверждают все четыре примера в правиле. В components/utils.ts:13 тег @param идёт сразу после текста расширенного контекста без пустой строки `*` — file:line подтверждается точно; тот же паттерн во всех остальных JSDoc-блоках файла (строки 37, 67, 93, 135, 163, 188, 220, 269, 305). Проверка по всему проекту (git ls-files по components/**и app/**) показала: 361 первый @param предварён текстом и 0 — пустой строкой, в 281 файле, то есть «систематически» — точная характеристика. Нормализация «в другом месте» к форматированию комментариев неприменима; незакоммиченные правки рабочего дерева (SignInForm, VerificationForm, FormInput) картину не меняют — подсчёт сделан по текущему дереву. Severity minor адекватна: это реальное нарушение структуры блока по правилу, но без какого-либо видимого эффекта для пользователя или данных.
- **[НЕЗНАЧИТЕЛЬНО]** 🟡 `components/layout/profile-page/components/SignOutButton.tsx:28` — Однострочный JSDoc над handleLogout не расширен до полной формы при редактировании файла (файл изменён в текущем рабочем дереве).
  - *Рек.:* При текущей правке SignOutButton.tsx расширить JSDoc handleLogout до полной формы; остальные 11 однострочных блоков расширять при следующем редактировании их файлов.
  > Проверка: Правило jsdoc.md действительно требует: «When editing a file where a function already has a single-line JSDoc without @param/@returns — extend it to the full form», а полная форма начинается с «Name — what it does» через em-dash. В текущем дереве на строке 28 SignOutButton.tsx над handleLogout по-прежнему однострочный JSDoc «/** Handle user logout, then navigate to the home page. */» без формата «handleLogout — …», при этом файл редактировался дважды (коммиты 0.1.19 и 0.1.23 от 2026-07-14, в 0.1.23 менялось само тело handleLogout — authenticate()→logout()) и блок расширен не был — суть находки верна. Однако деталь «файл изменён в текущем рабочем дереве» на сегодня неверна: git status показывает файл чистым, правки закоммичены в 0.1.23 (2026-07-14 18:11), т.е. рекомендация «при текущей правке» устарела — расширять нужно при следующем редактировании. Кроме того, у handleLogout нет параметров и нет возвращаемого значения (async, Promise<void>, а @returns по правилу нужен «always when there is a return value»), поэтому «полная форма» сводится лишь к формату первой строки с именем и em-dash — добавлять @param/@returns нечего. Severity minor адекватна шкале («нарушение без видимого эффекта сейчас»). **Уточнение:** Однострочный JSDoc над handleLogout (SignOutButton.tsx:28) не был расширен до полной формы при правках файла (последняя — коммит 0.1.23 от 2026-07-14; в текущем рабочем дереве файл уже закоммичен и не изменён). Функция без параметров и без возвращаемого значения, поэтому расширение сводится к первой строке формата 'handleLogout — …' без @param/@returns; выполнить при следующем редактировании файла.

### `rule:performance-rtk` — осталось: 0 🔧 (2026-07-18: decoupling задокументирован)

- **[НЕЗНАЧИТЕЛЬНО]** ✅ `app/store/providers/AuthContext.tsx:88` — Данные getMe из RTK Query дублируются в локальном состоянии контекста (useState user + setUser).
  - *Рек.:* Читать данные пользователя напрямую из состояния хука (второй элемент кортежа useLazyGetMeQuery содержит data) или из селектора кэша RTKApi.endpoints.getMe.select(), а локальный useState для user убрать — оставить только производные флаги isAuth/isLoading.
  > Проверка: Правило performance-rtk в разделе Anti-patterns прямо запрещает 'Duplicating RTK Query state in local component state — re-renders on every fetch, breaks deduplication. Read directly from the hook.' В AuthContext.tsx:88 заведён локальный useState<IUserEntity>, который наполняется ответом getMe: setUser(res.data) (стр. 119) и setUser(undefined) при logout (стр. 252). Хук useLazyGetMeQuery (стр. 107) при этом деструктурирует только { isError, error }, а data не читает — то есть данные пользователя действительно дублируются из кэша RTK в локальный state контекста. Формулировка находки, строка 88 и severity=minor точны: видимого сбоя нет, код работает. Нюанс: конкретные вреды из правила здесь ослаблены — local user обновляется только на явных checkToken (init/login/refetch), а НЕ на каждом 3s-поллинге, поэтому 're-renders on every fetch' не проявляется; наоборот, локальный state декуплирует экспонируемого user от опроса, и наивное 'читать data прямо из хука' дало бы ре-рендер всех потребителей AuthContext каждые 3с. 'breaks deduplication' тоже не применимо: AuthProvider один, подписчик getMe единственный. Тем не менее сам документированный анти-паттерн (дублирование состояния RTK в useState) в коде присутствует, эффект нулевой — что и соответствует minor.
>
### `rule:performance-streaming` — осталось: 0 🔧 (2026-07-18: force-static задокументирован)

- **[СЕРЬЁЗНО]** 🟡 `app/booking/page.tsx:32` — Тяжёлые блоки страниц не вынесены в отдельные async-компоненты под локальный <Suspense> — вся страница блокируется самым медленным запросом.
  - *Рек.:* На каждой из этих страниц оставить в page.tsx только быстрые запросы (getPageByUrl, getDictionary), а тяжёлый блок (каталог, wizard-данные, галерея) вынести в отдельный async server-компонент и обернуть в <Suspense fallback={<Skeleton/>}> — по образцу app/masters/[handle]/page.tsx.
  > Проверка: Правило performance-streaming (раздел «⚠️ Stream slow blocks of the page through local <Suspense>» и чеклист) прямо рекомендует: если латентность одного блока в 2+ раза выше остальных — вынести его в отдельный async-компонент под <Suspense>. В app/booking/page.tsx:32 все три чтения (getDictionary, getPageByUrl('booking'), getBookingData) собраны в один Promise.all и полностью awaited до рендера; getBookingData тяжёлый (getServicesCatalogData делает ~1 дерево + salons + 4 подкатегории + ~19 запросов продуктов, плюс getAdminsInfo на 32 админа и повторный salons) — очевидно 2x+ тяжелее getPageByUrl/getDictionary. Локального <Suspense> нет, loading.tsx для booking тоже нет (Glob дал только app/gallery/[handle]/loading.tsx), при том что сам код использует паттерн стриминга в app/masters/[handle]/page.tsx (Suspense + MasterLoader/PortfolioGridLoader). Обработки в другом месте нет; коммит 0.1.24 правил только booking-data.ts (5 строк), структуру стриминга не менял — значит не ALREADY_FIXED. Фактическая суть находки верна, но «major» завышен: страница рендерится корректно и деградирует штатно, эффект — только мягкая задержка первого рендера (Next держит старую страницу во время навигации), без ущерба данным/безопасности/функционалу; при текущем небольшом объёме CMS латентность умеренная. Поэтому PARTIAL с severity=minor. **Уточнение:** Тяжёлый блок booking-визарда (getBookingData: каталог + мастера + салоны) awaited в page.tsx до первого рендера, без локального <Suspense> и без loading.tsx — страница не стримится по образцу app/masters/[handle]/page.tsx. Реально, но это перф-оптимизация без корректностного/data/security-эффекта, поэтому severity ближе к minor. (severity → НЕЗНАЧИТЕЛЬНО)
>
### `skill:create-auth` — осталось: 0 ✅ (закрыто 2026-07-18)

- **[НЕЗНАЧИТЕЛЬНО]** 🟡 `components/forms/SignUpForm.tsx:180` — formIdentifier 'reg' и маркер провайдера 'email' захардкожены; getAuthProviders реализован в RTK, но нигде не используется
  - *Рек.:* Получать провайдеров через useGetAuthProvidersQuery, брать identifier/formIdentifier/isCheckCode из ответа (с фолбэком на 'email'/'reg'), а при пустом списке провайдеров скрывать формы авторизации (деградация без ошибок).
  > Проверка: Скилл create-auth (Step 4 props + Step 6, правило #6: 'formIdentifier is taken from provider.formIdentifier, not hardcoded'; markers берутся из getAuthProviders()) действительно требует не хардкодить formIdentifier и маркер провайдера. В SignUpForm.tsx это правило нарушено и сейчас: formIdentifier: 'reg' (стр. 182), signUp('email', ...) (стр. 197), auth('email', ...) (стр. 213) захардкожены, useGetAuthProvidersQuery в компоненте не импортируется и не используется — эта часть находки CONFIRMED. Однако вторая часть формулировки ('getAuthProviders реализован в RTK, но нигде не используется') на текущем рабочем дереве неверна: query используется в SignInForm.tsx:93 (строит табы из credential-провайдеров) и VerificationForm.tsx:49 (берёт systemCodeLength/systemCodeTlsSec) — это правки незакоммиченного дерева от 15.07. Живая проверка CMS (inspect-auth-providers.mjs) показала, что провайдеры теперь заведены: email (formIdentifier='reg', isCheckCode=false) и google (oauth) — снимок в CLAUDE.md про '[]' устарел. Захардкоженные значения точно совпадают с реальными CMS (email/reg/isCheckCode=false), поэтому видимого эффекта для пользователя нет — severity minor корректна. Итого: суть (хардкод маркеров в SignUpForm вопреки скиллу) верна, но клауза 'нигде не используется' фактически ошибочна, а строка 180 указывает на закрывающую скобку notificationData вместо 182/197 — отсюда PARTIAL. **Уточнение:** SignUpForm.tsx: formIdentifier 'reg' и маркер провайдера 'email' захардкожены (стр. 182/197/213) вместо взятия из useGetAuthProvidersQuery — вопреки скиллу create-auth. Оговорка 'getAuthProviders нигде не используется' неверна: query уже применяется в SignInForm и VerificationForm; значения совпадают с реальным CMS (email/reg/isCheckCode=false), видимого эффекта нет.
>
### `skill:create-cart-manager` — осталось: 0 ✅ (закрыто 2026-07-18)

- **[НЕЗНАЧИТЕЛЬНО]** ✅ `app/store/providers/AuthContext.tsx:179` — Мёртвый недостроенный путь синхронизации корзины через user.state.cart: эффект «Load cart from user state» ничего не загружает, а пишущая сторона никем не вызывается
  - *Рек.:* Либо достроить путь (писать корзину мутацией useUpdateUserStateMutation в местах изменения CartSlice и реально гидрировать Redux из user.state.cart в эффекте; при этом сохранять исходные типы formData, а не 'string'), либо перейти на нативный cart API по рецепту, либо — минимум — исправить вводящий в заблуждение комментарий «Load cart from user state» и пометить updateUserState как незадействованный легаси (удалять без явной просьбы пользователя нельзя по конвенциям проекта).
  > Проверка: Скилл create-cart-manager описывает менеджер корзины на нативном серверном API (Users.getCart/addCartItem/...) с реальной гидрацией из ответа сервера. Находка же указывает на конкретный мёртвый под-путь синхронизации через user.state.cart. Проверка кода: эффект «Load cart from user state» (AuthContext.tsx, ныне строки 186-192) не грузит корзину в Redux — читает user.state.cart лишь как условие и один раз диспатчит setCartVersion(1); поле version (cartReducer.version) больше нигде не читается, кроме гейта этого же эффекта (строка 95), то есть флаг-пустышка. Пишущая сторона также мертва: updateUserState/clearUserState и хук useUpdateUserStateMutation только определены и экспортированы (RTKApi.ts:395/445, updateUserState.ts:73), но grep по всему проекту не нашёл ни одного вызова/импорта в компонентах — реальная корзина работает на Redux+redux-persist (CartSlice). Строка 179 из аудита неточна лишь из-за дрейфа версий: в коммите 0.1.23 (дата аудита) тело эффекта начиналось ровно на 179, в 0.1.24 сдвинулось из-за добавленного JSDoc, сам код не менялся — не ALREADY_FIXED. Severity minor корректна: это мёртвый/недостроенный код и вводящий в заблуждение комментарий без видимого эффекта на пользователя сейчас.
>
### `skill:create-checkout` — осталось: 0 ✅ (2026-07-18: маркеры→константы; динамический путь неприменим — CMS; DateTimeStep уже schedule-driven)

- **[НЕЗНАЧИТЕЛЬНО]** 🟡 `components/layout/booking-page/useBookingSubmit.ts:129` — Маркер storage 'orders', formIdentifier 'order' и маркеры полей формы ('master', 'order_salon', 'interval') захардкожены — рецепт предписывает брать formIdentifier из getAllOrdersStorage(), а поля из getFormByMarker
  - *Рек.:* Минимум — вынести маркеры в константы одного модуля; лучше — получать formIdentifier из Orders.getAllOrdersStorage() (или getOrdersStorageByMarker('orders'), эндпоинт уже есть в RTKApi:318) и сверять отправляемые маркеры полей с getFormByMarker(formIdentifier), когда форма 'order' будет наполнена полями в админке.
  > Проверка: Рецепт create-checkout прямо требует (Step 1, Step 5 п.2): «formIdentifier is taken from storage, NOT hardcoded», а поля формы — из getFormByMarker(formIdentifier). Код в useBookingSubmit.ts:129 хардкодит createOrder('orders', {formIdentifier:'order', paymentAccountIdentifier:'cash', ...}) и вручную собирает formData с маркерами 'master'/'order_salon'/'interval'. Факты находки точны: маркеры захардкожены, рецепт предписывает иное. Однако живой запрос к CMS (getAllOrdersStorage под тест-юзером + getFormByMarker('order')) показал: единственное хранилище имеет identifier='orders', но formIdentifier=null и paymentAccountIdentifiers=[]; форма 'order' (id=1) существует, но её attributes — пустой объект {} (0 полей). То есть буквальное следование рецепту сломало бы создание заказа (null formIdentifier), а деривация полей из формы вернула бы пустой список. Значит хардкод — это осознанный работающий обходной путь, а не латентный баг: сейчас приложение создаёт заказ корректно. Эффекта для пользователя нет, и рецептный «правильный» путь неприменим, поэтому severity 'minor' (нарушение без эффекта) завышен — это чистая рекомендация/улучшение уровня info (в лучшем случае вынести маркеры в константы одного модуля; сверку с getFormByMarker имеет смысл делать только когда форму 'order' наполнят полями). Суть находки верна, но severity неточна — PARTIAL с понижением до info. **Уточнение:** Маркеры 'orders'/'order' и поля 'master'/'order_salon'/'interval' действительно захардкожены, и рецепт create-checkout это не рекомендует. Но живая проверка CMS показала, что рецептный динамический путь СЕЙЧАС нежизнеспособен: storage.formIdentifier = null, а форма 'order' не имеет полей (attributes = {}). Взятие formIdentifier из getAllOrdersStorage() дало бы null и сломало заказ, а выборка полей из getFormByMarker('order') не дала бы ни одного маркера. Хардкод здесь — функционально верный и единственный работающий вариант; это скорее рекомендация по вынесению маркеров в константы (info), чем нарушение с эффектом. (severity → ИНФО)
- **[НЕЗНАЧИТЕЛЬНО]** 🟡 ЧАСТИЧНО ИСПРАВЛЕНО (2026-07-18) `components/layout/booking-page/components/DateTimeStep.tsx:169` — Слоты времени — статический моковый массив TIMES, доступность слотов из timeInterval-атрибута формы не читается
  - *Сделано (сверка 2026-07-18):* слоты теперь читаются из расписания сущностей — `master_schedule`/`salon_time` (timeInterval) разворачиваются через `daySlots` в `useBookingWizard`; `DateTimeStep` рендерит `hasSchedule ? slots : TIMES`, т.е. `TIMES` — только фолбэк при отсутствии расписания. Из формы `order` слоты по-прежнему не читаются (у формы нет полей), но функционально доступность уже schedule-driven.
  - *Рек.:* При наполнении формы 'order' атрибутом interval (timeInterval) читать слоты через getFormByMarker → `localizeInfos.intervals[*]` и разворачивать в конкретные `[start,end]` пары новым экспортом SDK **`expandTimeIntervals(schedule, { from, to })`** (oneentry ≥ 1.0.156), оставив TIMES как фолбэк на случай пустой формы. ⚠️ **Обновлено 2026-07-17:** прежний путь `…intervals[*].timeIntervals` больше не работает — в SDK 1.0.156 авто-поле `timeIntervals` **удалено** (breaking): оно материализовало год слотов в каждом ответе и раздувало кэш. Теперь слоты резолвятся по требованию: для атрибута сущности (`master_schedule`/`salon_time`) — `expandAttributeTimeIntervals(attr, { from, to })`, для schedule формы — `expandTimeIntervals`. Есть type guard `isTimeIntervalAttribute`. Наш код удалённое поле не использовал — обновление ничего не сломало (tsc 0).
  > Проверка: Скилл create-checkout (Step 3) действительно описывает, что слоты timeInterval берутся из формы: localizeInfos.intervals[*].timeIntervals, а не из хардкода. По коду DateTimeStep.tsx:169 слоты рендерятся из статического TIMES (constants.ts), busyTimes всегда []; нигде (ни в useBookingSubmit, ни в index.tsx, ни в utils) слоты из формы не читаются — фактическая часть находки верна. Однако живой запрос inspect-forms.mjs показал, что форма 'order' имеет attributes={} (полей нет вообще), т.е. timeInterval-атрибута в CMS не существует — читать доступность неоткуда, а сама рекомендация помечена условной ('при наполнении формы order атрибутом interval'). Submit при этом корректно собирает и отправляет поле interval (timeInterval) в createOrder, так что заказ создаётся. Видимого эффекта нет и не может быть, пока форму не наполнят, а фолбэк tileDisabled в скилле при пустых интервалах и так ничего не блокирует — это делает находку рекомендацией/улучшением (info), а не нарушением с эффектом (minor). Строка 169 указана точно. **Уточнение:** DateTimeStep рендерит статический массив TIMES вместо слотов из timeInterval-атрибута формы; но у формы 'order' в CMS сейчас НЕТ ни одного поля (live: attributes={}), timeInterval-атрибута нет — читать нечего. Это forward-looking улучшение (когда форму наполнят), а не текущий дефект → severity info. (severity → ИНФО)
>
### `skill:create-product-list` — осталось: 0 ✅ (закрыто 2026-07-18)

- **[ИНФО]** 🟡 `app/api/api/RTKApi.ts:101` — RTK-эндпоинты getProducts и getProductsByPageUrl вызывают SDK без userQuery (offset/limit) — действует серверный дефолт limit 30, список молча усекается
  - *Рек.:* Добавить в аргументы эндпоинтов offset/limit и передавать userQuery в SDK-вызовы (как в серверных обёртках app/api/server/products/*); `{ error: null }` заменить на осмысленный объект ошибки.
  > Проверка: Скилл create-product-list (Step 3.2) действительно демонстрирует вызов Products.getProducts/getProductsByPageUrl с buildQuery(offset, limit) — то есть userQuery с offset/limit обязателен. Факт по коду верен: в RTKApi.ts:101 getProducts(body) и :121 getProductsByPageUrl(url) вызываются без locale и без userQuery, поэтому применился бы серверный дефолт limit и длинный список усёкся бы. Однако хуки useGetProductsQuery/useGetProductsByPageUrlQuery НИГДЕ не потребляются: grep по всему проекту показывает их только в определении RTKApi.ts и реэкспорте app/api/index.ts (плюс docs) — ни один компонент их не вызывает. Реальную загрузку каталога (~77 услуг) выполняют серверные обёртки app/api/server/products/getProducts.ts и getProductsByPageUrl.ts, где userQuery с offset/limit передаётся корректно. Поэтому формулировка «список молча усекается» (настоящее время, как о действующем эффекте) неточна — это латентный дефект в мёртвом коде, без текущего пользовательского эффекта. Severity info адекватна именно как рекомендация; суть находки верна, но описание переоценивает актуальность. **Уточнение:** RTK-эндпоинты getProducts/getProductsByPageUrl (RTKApi.ts:101,121) вызывают SDK без userQuery (offset/limit), что расходится с рецептом скилла и серверными обёртками; но это латентный дефект — хуки useGetProductsQuery/useGetProductsByPageUrlQuery нигде не потребляются (мёртвый код), реальная загрузка каталога идёт через app/api/server/products/* с корректным limit, поэтому усечения списка сейчас не происходит.
>
## 4. Опровергнутые и осознанные отклонения

**Опровергнуто проверкой (действий не требует):**

- 🔧 `components/layout/booking-page/useBookingSubmit.ts:131` (rule:orders, skill:create-checkout) — «Хардкод `paymentAccountIdentifier: 'cash'` вместо выбора оплаты при 2+ аккаунтах». **ОСОЗНАННОЕ ОТКЛОНЕНИЕ, обосновано дизайном** (пересмотрено 2026-07-17).
  - *Исходно было опровергнуто* тем, что `storage.paymentAccountIdentifiers` пуст и клауза «2+ → показать выбор» не наступает. **Эта премисса устарела:** 2026-07-17 пользователь привязал к хранилищу ОБА аккаунта (`cash` + `stripe`, `isUsed: true`) — формально клауза наступила.
  - *Но правка всё равно не нужна — так решено в дизайне.* Сверено с версткой (`static-html/`, источник истины): **выбора оплаты в букинге нет ни в одном варианте флоу**. Шаги — `["specialist","service","datetime"]` / `["salon","specialist","datetime"]` / `["specialist","salon","service","datetime"]`, платёжного шага нет; `completeBooking()` сразу показывает модалку «Booked! Your appointment has been confirmed. We'll send you a reminder» — про оплату ни слова; слов `payment`/`cash`/`stripe` в верстке нет вовсе. Это запись на приём с оплатой на месте, а не e-commerce-чекаут, на который рассчитано правило.
  - *Сам рецепт это допускает:* «Если оплата на месте — осознанное продуктовое решение, зафиксировать это как документированное отклонение».
  - *Пересмотреть,* если появится онлайн-оплата как продуктовое решение (`stripe` к хранилищу уже привязан — техническая часть готова): тогда шаг выбора оплаты нужно сперва спроектировать, а `useCreateOrder` уже умеет `createSession` + редирект (whitelist онлайн-провайдеров — `app/api/utils/isOnlinePayment.ts`).
- ⛔ `components/layout/booking-page/useBookingSubmit.ts:130` (rule:orders) — «Брать `formIdentifier` из `getAllOrdersStorage()`». Рекомендация **неисполнима и вредна**: у единственного storage `formIdentifier = null` — взяв его, код отправил бы `null` и сломал `createOrder`. Захардкоженный `'order'` — единственный рабочий вариант (маркеры вынесены в `app/store/orderMarkers.ts`).
- ⛔ `components/layout/gallery-grid/components/GalleryGrid.tsx:7` (rule:performance-bundle) — «photoswipe статически тянется в чанки галереи и мастера». Ложно: `gallery-grid` не импортируется ниоткуда, живая галерея — `gallery-page`, портфолио — `portfolio-grid/index.tsx` → `PortfolioGallery`. Неимпортируемый модуль в module graph не входит.
- ⛔ `next.config.ts:5` (rule:performance-streaming) — «Включить PPR через `experimental: { ppr: 'incremental' }`». Правило подаёт PPR как опциональный, а рекомендация **сломала бы сборку**: в next@16.2.10 ключ `experimental.ppr` удалён (`HardDeprecatedConfigError` — «merged into `cacheComponents`»).
- ⛔ `app/api/server/users/updateUserState.ts:25` (rule:server-actions) — «User-authorized обёртки лежат в `app/api/server/`». Правило регламентирует СПОСОБ вызова, а не структуру папок: ни `'use server'`, ни `server-only` в этих файлах нет, все вызывающие — клиентские, т.е. требование правила выполнено. `app/api/server/` — задокументированная конвенция проекта для всех SDK-обёрток.

- ⛔ `app/api/api/api.ts:11` (rule:nextjs-pages) — Локаль не берётся из params роута: захардкожена константа LANG_CODE = 'en_US', сегмента [locale] в App Router нет.

**Отклонено ПО ЗАМЕРУ (🟨) — находка фактически верна, но цена правки выше выгоды:**

- 🟨 `app/store/providers/AuthContext.tsx:8` (rule:performance-bundle) — **SDK OneEntry в клиентском бандле**. *Замер (prod-билд + инспекция чанков, 2026-07-16):* отдельный чанк **562 КБ raw / 121 КБ gzip = 28%** всего клиентского JS, подключён в HTML **каждого** роута, включая гостей. Состав — только граф SDK (`zod`, `socket.io-client`, `defineOneEntry`), это НЕ общий vendor-чанк. *Опровергнуто попутно:* дело не в барреле `app/api/index.ts` — его разделение вес не уберёт. **Решение пользователя: не чинить** («150кб фигня»); правка (ленивый SDK через `await import()`) была реализована и откачена — цена в том, что она превращает синхронный `login()` в fire-and-forget в самой хрупкой подсистеме. *Рецепт на будущее:* `isError` — чистый type guard, выносится в свой модуль бесплатно; SDK-рантайм грузить через `await import('./api')` в двух статических путях из корневого layout — `AuthProvider` и `RTKApi.queryFn`.
- 🟨 `components/layout/masters-page/components/MasterCard.tsx:31` (rule:performance) — **картинки карточек мастеров не гейтятся по вьюпорту**. *Замер (живой браузер, /masters, 1280×800, 2026-07-16):* 11 карточек, загружены **все 11 — включая 7 закадровых**; нативный `loading="lazy"` их НЕ отложил (документ 2153px при вьюпорте 800px — всё в «щедром» пороге Chrome), т.е. **фактическая часть находки верна**. *Цена вопроса:* весь набор **82 КБ** (`decodedBodySize`; `transferSize` занулён кэшем), портрет ~**7 КБ**, закадровые ≈ **52 КБ**. **Решение: не чинить** — 52 КБ шум (для сравнения отклонён пакет в 121 КБ gzip), а цена — хук + IntersectionObserver + риск подгрузки на глазах на странице, которую листают ровно ради этих карточек. `sizes="238px"`, `loading="lazy"` и blur-плейсхолдер уже на месте. *Пересмотреть,* если карточек станет заметно больше или портреты потяжелеют.

**Осознанные отклонения (🔧) — оставлены как есть, зафиксированы:**

- ✅ `app/reviews/page.tsx:32` (rule:nextjs-pages) — Тело страницы /reviews целиком рендерится из локального мока (components/layout/reviews-page/data.ts), контент CMS-страницы 'reviews' в теле не используется (только в generateMetadata).
- ✅ `app/contacts/page.tsx:119` (rule:nextjs-pages) — Ряд UI-текстов и данных секций захардкожен в page-файлах без чтения из CMS/словаря: «Get in Touch», подзаголовок героя, статистика «Daily 10:00–22:00» и др.
- ⚠️ `app/api/utils/dictionaries.ts:29` (rule:localization) — Словарь UI-строк реализован на блоке system_content (getBlockByMarker) вместо предписанного правилом AttributeSet static_content (getAttributesByMarker), и без центрального хелпера t().
- ⚠️ `components/layout/contacts-page/BookCtaBanner.tsx:30` (rule:localization) — Часть UI-микрокопи захардкожена в JSX без словаря (BookCtaBanner «Ready to visit us?», PromoBanner и др.) — помечено в коде как переходное состояние до наполнения system_content.
- ⚠️ `app/api/index.ts:1` (rule:performance-bundle) — Баррель app/api/index.ts реэкспортирует SDK-синглтон, RTK Query и все 19 серверных обёрток одним файлом; из него импортируют ~80 файлов, включая клиентские компоненты.
- 🟡 `components/shared/Image.tsx:1` (rule:linting) — Системный обход @next/next/no-img-element: 16 вхождений <img> в 11 файлах через eslint-disable
- ✅ `app/store/providers/AuthContext.tsx:233` (rule:linting) — Осознанное подавление exhaustive-deps с задокументированным обоснованием (isAuth намеренно исключён из deps)
- ⚠️ `app/api/api/api.ts:106` (skill:setup-oneentry) — reDefine() не принимает параметр langCode, хелпер getLang() не портирован — упрощение под одноязычный сайт
- ⚠️ `components/data.js:1` (rule:mismatch-log) — MISMATCH-LOG.md отсутствует в корне проекта при множестве активных триггеров правила (фолбэки/моки для отсутствующих CMS-сущностей, пустые формы, открытые вопросы к клиенту).
- ⚠️ `app/store/reducers/CartSlice.ts:84` (skill:create-cart-manager) — Корзина реализована по альтернативному паттерну Redux+redux-persist (клиентская), а не на нативном серверном cart API из рецепта
- ⚠️ `app/api/api/api.ts:11` (skill:create-page) — Локаль захардкожена константой LANG_CODE='en_US'; locale не берётся из params и langCode не передаётся в SDK-вызовы — вопреки шаблону рецепта
- ⚠️ `components/utils.ts:24` (skill:create-profile) — getFormAttributes отбрасывает attributes-объект целиком вместо Object.values — непустой объект с полями будет молча потерян
- ⚠️ `app/api/api/api.ts:11` (skill:create-profile) — Локаль не берётся из useParams и не передаётся в SDK-вызовы профиля — единая константа LANG_CODE
- ⚠️ `app/api/server/products/getProducts.ts:44` (skill:create-product-list) — langCode в SDK-вызовы каталога передаётся как undefined, локаль en_US зашита в конфиг defineOneEntry, locale из params не берётся
- ⚠️ `app/api/api/RTKApi.ts:271` (skill:create-form) — locale не передаётся в Forms.getFormByMarker — рецепт передаёт язык параметром
- ⚠️ `app/api/hooks/useSearchProducts.ts:50` (skill:create-search) — locale/langCode не передаётся в searchProduct — осознанное отклонение одноязычного проекта
- ⚠️ `components/utils.ts:220` (skill:create-menu) — flatMenuToNested строит дерево parentId-фильтром по плоскому списку и удаляет children — ровно тот паттерн, против которого предупреждает рецепт; при API-ответе в виде дерева заполненные children будут молча уничтожены
- ⚠️ `components/layout/bottom-menu/index.tsx:19` (skill:create-menu) — Запрашивается маркер 'bottom_web', заведомо отсутствующий в админке — постоянный лишний запрос с 404 при каждом рендере корневого лейаута
- ⚠️ `app/api/api/api.ts:11` (skill:create-menu) — Локаль зафиксирована константой LANG_CODE в конфиге SDK, langCode в Menus.getMenusByMarker не передаётся, ссылки меню без префикса /{locale}/ — осознанное отклонение одноязычного проекта
- ⚠️ `app/api/api/api.ts:11` (skill:create-product-card) — Локаль en_US захардкожена одной константой LANG_CODE, карточки не получают locale и ссылки не префиксуются локалью
- ⚠️ `app/api/api/api.ts:11` (skill:create-server-action) — Локаль зашита константой LANG_CODE='en_US' в конфиге SDK, параметр locale через серверные обёртки и вызовы не протаскивается

## 5. Пробелы покрытия аудита (сквозные аспекты и критик полноты)

Верификация находок завершена: **246/246** проверено адверсариально (см. разделы 2–3). Ниже — то, что выпало из разбиения по 42 измерениям: 5 сквозных аспектов интеграции (проверены отдельными агентами + адверсариальная верификация их находок) и 15 пробелов покрытия от критика полноты.

### 5.1. Сквозные аспекты интеграции OneEntry

#### Контракт серверных обёрток `{ isError, error?, data? }` (`wrappers-contract`)

- **[НЕЗНАЧИТЕЛЬНО]** ✅ `app/api/server/users/logOutUser.ts:13` — logOutUser не возвращает конверт { isError, error? }, а отдаёт { data } или { error } и никогда не бросает исключений
  - *Рек.:* Привести к общему контракту: возвращать { isError: boolean, error?: IError }, либо задокументировать в JSDoc, что функция никогда не бросает и всегда деградирует локально через finally, чтобы у потребителей не оставалось мёртвого try/catch.
  > Проверка: Проверил файл и всех потребителей — находка точна во всех пунктах. logOutUser (line 13) типизирован как Promise<object> и возвращает { data: result } или { error: message } без поля isError, тогда как ВСЕ прочие экспортируемые серверные обёртки (getProductById, getFormByMarker, updateOrderByMarkerAndId, getAdminsInfo, блоки, меню, страницы — проверено grep'ом) возвращают { isError, error?, payload? }; этот конверт прямо зафиксирован как конвенция в CLAUDE.md, так что logOutUser нарушает собственный документированный контракт проекта, и это единственное отклонение. Функция никогда не бросает: try/catch перехватывает всё в { error }, а finally всегда чистит локальную сессию (clearSession лишь пересоздаёт SDK-инстанс, не бросает) — поэтому try/catch в SignOutButton.tsx:30 и LogoutMenuItem.tsx:21 фактически мёртв, и оба потребителя не проверяют возвращаемое значение, не отличая успех от неуспеха. Видимого эффекта для пользователя нет, так как finally всегда сбрасывает сессию, а потребители безусловно делают logout()+redirect — это ровно minor по шкале (нарушение без видимого эффекта сейчас). Ни одно из документированных осознанных отклонений этот случай не покрывает.
- **[ИНФО]** 🟡 `app/api/server/users/updateUserState.ts:24` — updateUserState/clearUserState возвращают boolean вместо конверта и теряют детали ошибки SDK
  - *Рек.:* Либо оставить как есть, но задокументировать boolean-контракт как осознанное исключение для мутаций user.state; либо вернуть { isError, error? }, чтобы RTK-мутация могла проксировать ошибку в UI.
  > Проверка: Технические факты находки верны: updateUserState/clearUserState действительно возвращают Promise<boolean> и на isError(fresh)/неуспешном updateUser отбрасывают IError, а из app/api/index.ts функция не экспортируется — по форме это расходится с конвертными обёртками (getPageByUrl, updateOrderByMarkerAndId). Однако severity завышена: boolean-контракт осознанный и уже задокументирован (JSDoc «True on success, false on SDK error» и тип RTK-мутации build.mutation<boolean>), то есть половина рекомендации выполнена. Практического эффекта нет вообще: хук useUpdateUserStateMutation нигде не потребляется, а сам queryFn RTK оборачивает boolean в {data} и никогда не отдаёт {error}, так что «RTK-мутация не может показать причину сбоя» описывает несуществующий путь; предложенная правка к тому же неполна без переписывания RTKApi.ts. Плюс конвент внутри server/users/ и так неединообразен — рядом лежит logOutUser, который возвращает {data}/{error}. Итог: находка корректна как стилистическая заметка, но это рекомендация по консистентности без видимого эффекта, поэтому уровень info, а не minor. **Уточнение:** updateUserState/clearUserState намеренно возвращают boolean вместо конверта {isError,error?} — отклонение от конвенции обёрток чисто стилистическое: контракт задокументирован в JSDoc и в типе RTK-мутации, RTK-хук useUpdateUserStateMutation нигде не используется, а сам конверт в server/ и так неединообразен (logOutUser отдаёт {data}/{error}). Рекомендация по консистентности без runtime-эффекта.
- **[ИНФО]** 🟡 `app/api/server/pages/getPageByUrl.ts:42` — Ни одна обёртка не использует ключ `data` из задокументированного контракта — payload лежит под доменными ключами (page/block/products/...)
  - *Рек.:* Не менять код (ключи консистентны). Уточнить формулировку контракта в CLAUDE.md: конверт = { isError, error?, <domain-payload>?, [total] }, где payload лежит под доменным ключом, а не под общим `data`.
  > Проверка: Я прочитал все 20 файлов в app/api/server/**. Для перечисленных в находке обёрток (page, pages, block, blocks, slides, form, menu, admins, attribute, product, products, order, orders) вывод точен: payload лежит под доменным ключом, у пагинированных обёрток добавляется обязательный `total`. Однако центральное утверждение сводки «ни одна обёртка не использует ключ data» опровергается конкретным контрпримером: logOutUser.ts возвращает `{ data: result }` на успехе (и без `isError` вовсе). Также опровергается тезис о единообразии: updateUserState/clearUserState отдают bare boolean. Это уже зафиксировано и в MISMATCH-LOG.md:1473. Предложенная находкой переформулировка контракта тоже не описала бы logOutUser/updateUserState. Ядро (доменные ключи вместо `data`, доп. `total`) — валидное info-замечание по документации, но абсолютные формулировки неверны, поэтому PARTIAL, severity info без изменений. **Уточнение:** Большинство обёрток кладут payload в доменный ключ (page/block/products/…), а не в `data` — в этом наблюдение верно, и формулировка `{isError,error?,data?}` в CLAUDE.md действительно приблизительна. Но абсолютные утверждения находки ложны: `logOutUser` (app/api/server/users/logOutUser.ts:20) возвращает `{ data: result }` — ключ `data` используется; а набор обёрток НЕ единообразен: `logOutUser` отдаёт `{data}`/`{error}` без `isError`, `updateUserState`/`clearUserState` возвращают голый boolean.

#### Env-переменные подключения OneEntry (`env-config`)

- **[ИНФО]** 🟡 `app/types/env.d.ts:2` — Осталась легаси-декларация модуля '@env' с именами v1 (PROJECT_URL, APP_TOKEN), противоречащими канону
  - *Рек.:* Удалить неиспользуемую декларацию '@env' (или переименовать под актуальные NEXT_PUBLIC_ONEENTRY_* имена), чтобы в кодовой базе не оставалось отсылок к легаси PROJECT_URL/APP_TOKEN.
  > Проверка: Фактическая часть находки полностью подтверждена: файл действительно объявляет ambient-модуль '@env' с легаси-именами PROJECT_URL/APP_TOKEN, этот модуль нигде не импортируется (grep по всему репо, кроме node_modules и лога, пуст), ни tsconfig paths (только @/*→./*), ни babel (его нет), ни пакет dotenv не порождают модуль '@env'. Локальные const PROJECT_URL/APP_TOKEN в api.ts:4-5 — это просто имена переменных, читающих канонические process.env.NEXT_PUBLIC_ONEENTRY_URL/_TOKEN, и с декларацией никак не связаны, так что рантайм- и тайп-эффекта у файла нет. Опровергнуть по существу не удалось — это реальный мёртвый артефакт из v1 (коммит 0.1.0). Корректирую только severity: это не «нарушение» с последствиями, а невидимый в рантайме dead-code и чисто гигиеническая рекомендация к удалению/переименованию, то есть info; вдобавок проект осознанно держит неиспользуемые артефакты на диске («ничего не удалять без явной просьбы»), что дополнительно ослабляет саму рекомендацию. **Уточнение:** Мёртвая ambient-декларация модуля '@env' (PROJECT_URL/APP_TOKEN, имена v1) в app/types/env.d.ts — не импортируется нигде, тулингом не используется, эффекта нет; кандидат на чистку, а не нарушение.
- **[НЕЗНАЧИТЕЛЬНО]** ✅ `app/api/api/api.ts:4` — Нет валидации/fail-fast при отсутствии env: приведение `as string` маскирует undefined, SDK молча инициализируется с undefined URL/token
  - *Рек.:* Добавить проверку обязательных env при инициализации (явный throw с понятным сообщением, если NEXT_PUBLIC_ONEENTRY_URL/TOKEN пусты) вместо `as string`, чтобы отсутствие конфигурации диагностировалось сразу.
  > Проверка: Факты находки подтверждаются: в app/api/api/api.ts:4-5 `as string` действительно маскирует возможный undefined (нарушение строгого TS, декларируемого проектом), явной валидации обязательных env в проекте нет нигде (grep по throw/required/invariant пуст, config-модуля и проверок в next.config нет), а PROJECT_URL/APP_TOKEN без фолбэка уходят в defineOneEntry на строках 32/88/110. Дополнительно вскрывается непоследовательность: та же переменная в 4 других местах (layout.tsx:61, page.tsx:112, robots.txt, sitemap.xml) читается с фолбэком `|| 'https://oneentry.cloud'`, а в инициализации SDK — голый каст. Единственная неточность формулировки — «невнятные ошибки в рантайме»: реально серверные обёртки ловят исключения (isError) и компоненты деградируют молча, что находку не опровергает, а усиливает (молчаливую мисконфигурацию труднее диагностировать, чем явный fail-fast; graceful-degradation проекта касается пустой CMS-даты, а не отсутствия build-time конфига). Severity minor выставлена корректно: env фактически задан, видимого эффекта сейчас нет, но это настоящее нарушение TS-дисциплины и несогласованность в коде.

#### Ревалидация и инвалидация данных (`revalidation-invalidation`)

- **[ИНФО]** ✅ `app/profile/page.tsx:15` — Нет middleware.ts и серверной защиты приватного /profile — гейт только клиентский
  - *Рек.:* Оставить как есть допустимо (данные защищены токеном). Для defense-in-depth и UX рассмотреть middleware.ts, который по наличию refresh-сессии редиректит гостя с /profile на страницу входа вместо отдачи страницы с последующим клиентским '401 ERROR'. Если middleware сознательно не нужен — зафиксировать решение в CLAUDE.md.
  > Проверка: Опровергнуть не удалось: все факты находки подтверждаются. middleware.ts в проекте нет (Glob по любому уровню находит только node_modules), next.config.ts redirects() содержит лишь /password→/, а серверный ProfilePageLayout (app/profile/page.tsx:15-47) тянет только публичные данные (getPageByUrl('profile'), getAdminsInfo) и безусловно рендерит клиентский ProfilePage — серверного редиректа гостя нет. Приватные данные (заказы) действительно защищены токеном: ProfileHistory.tsx:46-48 фетчит getAllOrdersByMarker только при isAuth, а сам SDK-вызов требует access-token, живущий in-memory в SDK-инстансе — серверной утечки гостю нет, гейт клиентский (index.tsx:54 → AuthError «401»). Severity=info корректна: по шкале это именно рекомендация/улучшение, и находка сама признаёт отсутствие утечки и осознанную клиент-центричную архитектуру. Единственная неточность — в рекомендации: сессия хранится в localStorage (refresh-token) и in-memory (AuthContext.tsx, app/api/api/api.ts), серверного cookie нет, поэтому middleware-редирект «по наличию refresh-сессии» без введения cookie нереализуем; это ослабляет рекомендацию, но не опровергает суть находки и не меняет вердикт.
- **[НЕЗНАЧИТЕЛЬНО]** ✅ `app/api/api/RTKApi.ts:176` — Guard return { error: null } — неидиоматичный «skip», нарушающий единый формат конверта ошибки
  - *Рек.:* Не имитировать skip через { error: null }: пропуск задавать опцией skip/skipToken на стороне вызова (как уже в useCartItem), а ветку error оставлять только для настоящих IError, единообразно возвращая { error: result }. Тогда все эндпоинты имеют один формат конверта и хуки надёжно различают «ошибка» и «пусто».
  > Проверка: Находка точна во всех проверяемых утверждениях. По исходникам RTK 2.12.0 (dist/query/cjs/rtk-query.development.cjs) подтверждено: возврат `{ error: null }` из queryFn не бросает (строка 1016 `if (result.error) throw` — null falsy) и не триггерит dev-варнинг (строка 999 строго сравнивает с `void 0`), поэтому запрос завершается fulfilled с `data === undefined` — имитация skip через error-канал, а не состояние ошибки. Три формы конверта реально существуют: IError-объект, строка 'Data error' (строка 160) и null (guard-ы 119/176/196). Эффект действительно минимален: useCartItem дополнительно ставит skip (guard не достигается), OfferCard/OfferInfo/OfferDetailCard читают только data и деградируют на undefined, RTK-хук getProductsByPageUrl вообще не используется (guard 119 — мёртвый код), а UserForm ветвится на error лишь для getFormByMarker с корректным IError. Опровергнуть не удалось: это подлинное неидиоматичное отклонение без видимого эффекта — severity minor выставлена верно.

### 5.2. Пробелы покрытия от критика полноты

Направления, не закреплённые ни за одним из 42 измерений и 5 аспектов. Это **гипотезы охвата**, а не подтверждённые нарушения — каждую нужно отдельно проверить.

1. SEO-роуты не закреплены ни за одним правилом/скиллом: app/sitemap.xml/route.ts и app/robots.txt/route.ts строят базовый URL сайта из NEXT_PUBLIC_ONEENTRY_URL (это URL CMS beauty.oneentry.cloud, а не сайта), т.е. sitemap и Sitemap-строка robots.txt указывают на чужой домен — никто это не проверяет.
2. Метаданные из CMS вне покрытия: 16 страниц с generateMetadata (app/[handle]/page.tsx, app/services/[handle]/page.tsx и др.) и отсутствие metadataBase в app/layout.tsx — фолбэки, дублирование запроса getPageByUrl и OG-резолвинг метаданных не входят ни в одно из 42 измерений.
3. Обработка рантайм-ошибок на уровне сегментов не аудируется: во всём app/ нет ни одного error.tsx и global-error.tsx (есть только app/not-found.tsx и единственный app/gallery/[handle]/loading.tsx), т.е. необработанный сбой SDK роняет страницу без границы ошибки.
4. Санитизация/XSS CMS-контента не покрыта: dangerouslySetInnerHTML используется в app/layout.tsx, app/page.tsx, app/gallery/[handle]/page.tsx, app/masters/[handle]/page.tsx, app/services/[handle]/page.tsx и components/layout/home/catalog-grid/components/CategoryTile.tsx для htmlContent/JSON-LD из CMS — ни одно правило не проверяет экранирование.
5. Корректность JSON-LD structured data, собираемой из CMS-данных (app/layout.tsx:168 и детальные страницы), — сериализация, экранирование и валидность схемы не входят ни в правила, ни в сквозные проверки.
6. Диагностические поверхности в проде вне аудита: app/api/test-connection/route.ts (force-dynamic, исполняет SDK-вызовы по произвольным query-параметрам) и публичный дашборд app/api-test/* (page.tsx, ApiTestClient.tsx) — доступность и злоупотребление ими не проверяются.
7. next.config.ts как поверхность интеграции никем не закреплён: битый rewrite /fonts/:path*→ /api/fonts/:path* (роут app/api/fonts не существует — Glob пуст), security-заголовки без Content-Security-Policy, experimental.staleTimes (30/180с — влияет на свежесть CMS-данных на клиенте) и remotePatterns **.oneentry.cloud.
8. Unit-тесты интеграции не оцениваются: аудит включает только правило playwright-e2e, а jest-покрытие SDK-обёрток и слайсов (tests/jest/api.test.ts, utils.test.ts, CartSlice.test.ts, OrderSlice.test.ts, FormFieldsSlice.test.ts, Image.test.tsx) — его полнота и актуальность моков — отдельное непокрытое измерение.
9. Согласованность типов SDK с проектом не выделена в измерение: 205 импортов из oneentry/dist/* в 127 файлах плюс локальные app/types/global.d.ts и app/types/enum.ts — соответствие типов фактическим ответам CMS (например, price — строка «450 AED», attributes формы — массив ИЛИ объект) не сверяется системно.
10. Accessibility CMS-контента полностью выпала: alt-тексты у изображений из image-атрибутов CMS, aria/клавиатурная навигация hero-карусели (home_hero) и модалок, семантика вставляемого htmlContent — ни одно из 22 правил и 20 скиллов не про a11y.
11. Аудит статичен и не сверяет код с живым состоянием CMS: известные расхождения (компоненты components/pages/PaymentSuccess.tsx и PaymentCanceled.tsx с роутингом app/[handle]/page.tsx при отсутствующих в админке страницах payment_success/payment_canceled — редирект после оплаты Stripe уйдёт в notFound(); opening_time 404; пустой reviews_carousel) выявляются только инспекционными скриптами .claude/temp/audit-content-plan.mjs, которые в аудит не включены.
12. Сетевая устойчивость серверных обёрток не покрыта: таймауты, ретраи, AbortSignal и поведение сборки/страниц при полной недоступности beauty.oneentry.cloud — ни performance-правила, ни сквозной контракт { isError, error?, data? } этого не проверяют.
13. Безопасностный аспект экспозиции токена шире сквозной env-проверки имён: NEXT_PUBLIC_ONEENTRY_TOKEN вкомпилируется в клиентский бандл через app/api/api/api.ts (defineOneEntry с APP_TOKEN, RTK Query дергает SDK из браузера), и какие операции (заказы, пользователи, формы) этот токен разрешает произвольному посетителю — отдельно не аудируется.
14. Мутабельный синглтон SDK на сервере вне явного покрытия: app/api/api/api.ts держит module-level apiInstance, который reDefine()/clearSession() подменяют — риск протечки пользовательской сессии между параллельными серверными запросами лежит на стыке правил tokens/auth-provider и, судя по формулировкам измерений, ни одному не принадлежит целиком.
15. Наполняющий контур через внутренний admin API целиком вне аудита: скрипты .claude/temp/masters-common.mjs и fill-*.mjs (логин Playwright, креды OE_ADMIN_LOGIN/OE_ADMIN_PASSWORD в env, перезапись listTitles наборов с бэкапом) — часть интеграции проекта с OneEntry, не отражённая ни в правилах, ни в скиллах.

> Общая оценка критика: Разбиение 22 правила + 20 скиллов + 5 сквозных проверок хорошо покрывает «внутренность» интеграции с OneEntry (данные, атрибуты, формы, заказы, auth, перфоманс, локализация), но систематически упускает «обвязку» Next.js вокруг CMS и прод-поверхности. Подтверждённые кодом дыры покрытия группируются в шесть кластеров: (1) SEO из CMS — sitemap/robots с неверным базовым URL из NEXT_PUBLIC_ONEENTRY_URL, generateMetadata на 16 страницах, отсутствие metadataBase, JSON-LD; (2) поверхности ошибок — нет ни одного error.tsx/global-error.tsx; (3) безопасность — dangerouslySetInnerHTML для CMS-HTML в 6 файлах, публичные диагностические роуты test-connection/api-test, отсутствие CSP, объём прав NEXT_PUBLIC-токена в клиентском бандле; (4) конфигурация next.config.ts (битый fonts-rewrite, staleTimes, remotePatterns); (5) верификация — jest-тесты интеграции не оцениваются (есть только правило playwright-e2e), а аудит не сверяется с живым состоянием админки (payment_success/payment_canceled отсутствуют в CMS при живом роутинге — сломан пост-оплатный флоу); (6) a11y CMS-контента отсутствует как измерение вовсе. Оценка полноты: ~75% — статический код против правил SDK проверен плотно и с малым риском пропусков, но рантайм-/прод-аспекты и консистентность код↔живая CMS остались слепой зоной; рекомендую добавить 4–6 агентов по кластерам выше, в первую очередь SEO-роуты, error-границы и безопасность CMS-HTML/токена.

## C.6. Events — события CMS (ЗАКРЫТО 2026-07-16)

**Статус: ✅ ЗАВЕДЕНО И ПРОВЕРЕНО.** Пользователь создал 6 событий; сброс пароля проверен живым прогоном (`generate-code` → **201**).

| Идентификатор | Название | Назначение | Используется |
|---|---|---|---|
| `reset_password` | Reset password | Сброс пароля: выдаёт OTP и авторизует смену | `ForgotPasswordForm` (`generateCode`), `VerificationForm` (`checkCode`, resend), `ResetPasswordForm` (`changePassword`) — **одно событие на весь флоу** |
| `otp` | Registration otp | OTP активации аккаунта | `VerificationForm` resend при `action === 'activateUser'` — **сейчас дремлет**: активация выключена (`isCheckCode: false`) |
| `generate_otp` | Generate code | `formType: 'send_code'` | кодом **не используется** |
| `order`, `order_change`, `order_canceled` | — | уведомления по статусам заказа (`upcoming`/`completed`/`canceled`, storage id 1) | сервером, не фронтом |

**Важно:** фактические идентификаторы отличаются от типовых в правиле `auth-provider` (`reset_password`, а не `password_reset`; `otp`, а не `user_registration`) — константы в `components/forms/authEventMarkers.ts` приведены к фактическим.

**Почему маркеры нельзя подтвердить «пробником»** (стоило времени, не повторять):

- `Events.getAllEvents()` → **401** с токеном проекта, хотя правило `auth-provider` и скилл `create-subscription-events` называют метод публичным с SDK 1.0.150 (стоит 1.0.155). Скрипт: `.claude/temp/inspect-events-public.mjs`.
- `generateCode` отвечает `400 "User already has a code"`, пока у пользователя есть невыданный код — **несуществующее событие даёт ТУ ЖЕ ошибку**. Пробники порядко-зависимы: первый вызов выигрывает и маскирует все следующие.
- Код живёт **дольше** `systemCodeTlsSec` (120 с — кулдаун ресенда, не TTL кода; фактически истёк за ~14 мин).
- `checkCode` возвращает `false` на любой маркер, включая несуществующий, — неотличимо от неверного кода.
- Достоверно: список через внутренний admin API (`node .claude/temp/list-events.mjs`) + живой прогон флоу.
