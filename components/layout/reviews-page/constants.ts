/** Brand colors from the static-html mock (`ReviewsPage.tsx`). */
/** PINK — brand fuchsia (chips / master name / active filters). */
export const PINK = '#ed21f1';
/** PINK2 — brand fuchsia-bright (start of the active gradient). */
export const PINK2 = '#f60efb';
/** CYAN — rating / stars accent (distinct from the pink gradient). */
export const CYAN = '#109AA9';
/** DARK — primary body text. */
export const DARK = '#4c4d56';
/** INK — heading text / underline. */
export const INK = '#414253';
/** MUTED — secondary text (dates, counts). */
export const MUTED = '#a8a9b5';
